# Korean PowerPoint formatting

한글이 포함된 PowerPoint(`.pptx`/`.potx`) 또는 HTML-to-PPTX 작업에는
`pptx-korean-formatting` 스킬을 사용한다.

규칙의 단일 원본은 [SKILL.md](SKILL.md)이다. 이 파일에 규칙 본문을 복제하지 않는다.
스킬을 지원하지 않는 에이전트에서는 `SKILL.md`를 해당 도구의 프로젝트 규칙이나
지침 파일에 직접 연결한다.

---

Use the `pptx-korean-formatting` skill for PowerPoint (`.pptx`/`.potx`) or
HTML-to-PPTX work that contains Korean text.

[SKILL.md](SKILL.md) is the single source of truth. Do not duplicate its rules here.
For agents without Skill support, reference `SKILL.md` from the tool's project rules
or instruction file.
